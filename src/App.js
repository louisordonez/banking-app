import './App.css';
import Button from './Components/Button/Button.js';

const App = () => {
  return (
    <div>
      <Button variant="btn-primary" textValue="Confirm" />
    </div>
  );
};

export default App;
